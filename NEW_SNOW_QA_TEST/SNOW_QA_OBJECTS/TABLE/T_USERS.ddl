create or replace TABLE T_USERS (
	IDENTIFICATION_NUMBER NUMBER(38,0) NOT NULL,
	NAME VARCHAR(16777216),
	MIDDLE_NAME VARCHAR(50),
	LAST_NAME VARCHAR(500),
	ADDRESS VARCHAR(16777216),
	COMPUTER_IDENTIFICATION_NUMBER NUMBER(38,0),
	OCCUPATION BOOLEAN
);