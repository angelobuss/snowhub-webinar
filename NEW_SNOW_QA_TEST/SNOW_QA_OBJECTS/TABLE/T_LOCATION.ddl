create or replace TABLE T_LOCATION (
	AREA_CODE NUMBER(38,0) NOT NULL,
	X_COORDINATE NUMBER(10,0),
	Y_COORDINATE NUMBER(10,0),
	LOCATION_NAME VARCHAR(100),
	TERRAIN_AREA NUMBER(38,0),
	TERRAIN_VOLUME NUMBER(38,0),
	TERRAIN_TYPE VARCHAR(16777216),
	TERRAIN_WEIGHT NUMBER(38,0)
);